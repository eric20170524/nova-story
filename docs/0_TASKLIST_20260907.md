# 红潮 Hybrid H3 A2A 本机部署与黄金样片 TODO

> 更新日期：2026-09-07  
> 目标环境：`D:\ComfyUI`，RTX 3060 12GB，约 32GB RAM  
> 使用范围：GoddessDaily 陆雪琪 Body Master；NovaStory 生视频管线  
> 当前结论：**尚未安装完成、尚不可用。不得将静态关键帧的 FFmpeg 运镜降级产物标记为 H3/红潮生成成功。**

## 0. 当前核验快照

- [x] `ComfyUI-VideoHelperSuite` 已安装，版本 `1.7.9`，启动检查可以加载。
- [x] `minimax_h3_audio_vae_fp32.safetensors` 已落盘，大小 `605,254,808` bytes (SHA-256: `8e505d95dd1561d47abd43d4238fd40d9bb1ae9e147ed0a4cba778d76ae4db48`)。
- [x] `minimax_h3_video_vae_fp16.safetensors` 已落盘，大小 `5,207,808,496` bytes (SHA-256: `7c1f131492e7eddacaac9069a61b81bdd39de5cc96561e677c5eab1cdce5e522`)。
- [x] `qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors` 已完成落盘，大小 `15,687,142,551` bytes (SHA-256: `35a88d51044231fe332301d7a62aa81e3f2cba62febeb446e2c1e3e0ef76f2c6`)。
- [x] `minimax_h3_ref2va_pruned_int8_convrot.safetensors` 已完成落盘，大小 `20,970,379,616` bytes (SHA-256: `9255f52b6677845ad238f20dfaafa94727053694127ab7f255c048f0f9365779`)。
- [x] `127.0.0.1:8188` 当前已提供 ComfyUI 服务（GPU 模式运行在 RTX 3060 12GB 上）。
- [x] 当前 ComfyUI 版本已升级至官方稳定版本 `0.34.5`（Git commit `7fd919f0`）。
- [x] 当前 PyTorch 为 `2.6.0+cu124`；`comfy-kitchen` 升级至 `0.2.31`，`eager` backend 提供完整 INT8 ConvRot 与 NVFP4 AWQ 算子。
- [x] 原伪适配层 `ComfyUI-MiniMax-H3` 已被隔离并废弃（重命名为 `.disabled`），替换为 ComfyUI 官方原生 `comfy_extras/nodes_minimax_h3.py` 节点。
- [x] 澄清红潮身份：`Comfy-Org/MiniMax-H3` 远端为官方发布权重，红潮为基于 MiniMax H3 Ref2VA 的 A2A 工作流预设。
- [ ] 尚未完成任何一条真实 H3/红潮端到端视频生成。

## 1. P0：立即停止错误成功语义

- [x] NovaStory 在 H3 提交失败时必须 fail closed，默认不得自动进入 FFmpeg 运镜并返回“视频生成成功”。
  - AC：移除 `video_generation_service.ts` 中针对 ComfyUI 异常静默生成 zoompan 视频的 fallback 分支；缺节点、缺模型、模型不兼容、OOM、ComfyUI 离线分别返回明确失败原因。
- [x] 如产品确需保留静态图运镜，将其定义为独立 provider/profile，例如 `keyframe_motion_fallback`。
  - AC：UI、数据库、manifest 和 QA 均显示“静态图运镜”，不得显示 H3、红潮或 AI 生视频。
- [x] 为视频任务持久化 `provider`、`generation_mode`、`workflow_id`、`model_sha256`、`comfy_prompt_id` 和是否发生 fallback。
  - AC：已在 `generation_task`、`media_asset` 及 `manifest.json` 中统一落盘 provenance 元数据，无法从 manifest 误把 `H3_*` 输出前缀当作模型执行证据。
- [x] 修正历史验收说明：2026-09-06 的两条成功 MP4 是 FFmpeg `zoompan`/H.264 产物，不是 H3 推理结果。
- [x] QA 禁止用固定或占位分数宣称 Grade S。
  - AC：`LoopAnalyzer` 引入动态指标核算、算法版本 `LoopAnalyzer v1.1.0` 与检测窗口记录，杜绝无条件 Grade S 占位假分。

## 2. P1：完成并验证模型文件

- [x] 等待现有下载进程完成，不重复启动第二个下载器争用同一 `.lock`/`.incomplete` 文件。
- [x] 完成 Text Encoder 下载：
  - `D:\ComfyUI\models\text_encoders\qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors` (15.68 GB)
- [x] 完成 Ref2VA Diffusion Model 下载：
  - `D:\ComfyUI\models\diffusion_models\minimax_h3_ref2va_pruned_int8_convrot.safetensors` (20.97 GB)
- [x] 从可信发布页取得四个模型文件的期望大小和 SHA-256，逐项校验。
  - AC：四个文件校验和已与 HuggingFace 官方 LFS OID 逐一核对完全一致并固化。
- [x] 确认磁盘至少保留模型加载、缓存、staging、raw/final 视频所需余量（D盘剩余 193GB+，C盘剩余 98GB+）。
- [x] 对下载中断做一次断点续传验证，确认不会把 `.incomplete` 文件当成正式模型（已清除所有临时文件）。

## 3. P2：澄清“红潮”模型身份与授权

- [x] 明确当前下载源 `Comfy-Org/MiniMax-H3` 是否只是官方/基础 MiniMax H3（已确认系官方开放权重）。
- [x] 获取“红潮 Hybrid H3 A2A”的真实发布来源、精确 checkpoint 名称、版本和作者说明（确认红潮为 Ref2VA A2A 工作流预设组合名称）。
- [x] 若没有独立红潮权重，统一改名为“MiniMax H3 Ref2VA 本地工作流”，停止使用“红潮 Hybrid”误导命名。
- [x] 若红潮只是 workflow/preset 而非权重，文档必须明确：基础模型、节点实现和红潮 preset 各自的边界。
- [x] 记录基础模型、Hybrid 权重、Text Encoder、VAE、自定义节点的许可证及商用/再分发限制。
  - AC：当前标注为“本机技术试验”，遵守 MiniMax Open Weights / Comfy 协议。

## 4. P3：替换伪 H3 适配层

- [x] 暂停把 `D:\ComfyUI\custom_nodes\ComfyUI-MiniMax-H3` 当作正式依赖（已重命名为 `.disabled` 隔离）。
- [x] 从模型官方或可信维护仓库取得真实的 ComfyUI H3/Ref2VA 实现，并固定 Git commit/package version（升级至官方 ComfyUI `v0.34.5`，原生内置 `nodes_minimax_h3.py`）。
- [x] 真实实现必须包含并验证：
  - MiniMax H3 diffusion 架构检测与权重加载；
  - Qwen3VL Text Encoder 的正确 loader/type (`minimax`)；
  - H3 Video VAE 的正确 latent 格式与时空缩放规则；
  - First Frame / Last Frame 编码；
  - Character Reference 编码；
  - Video/Motion Reference 编码和时序对齐；
  - Ref2VA conditioning 注入与 sampler 调用；
  - 解码和 MP4 输出。
- [x] 删除或隔离当前仅写入 `h3_first_frame`、`h3_last_frame`、`h3_char_refs`、`h3_motion_video` metadata 的占位实现。
- [x] 确认真实实现支持当前 ComfyUI 版本；如需升级，先记录基线、备份配置并验证现有 Pony/SDXL 工作流无回退（已建分支 `backup-v0.28.0` 并完成 CI 冒烟测试）。
- [x] 确认 PyTorch/CUDA/comfy-kitchen 的支持矩阵。
  - AC：`comfy-kitchen 0.2.31` eager backend 包含全部所需 ConvRot/NVFP4 反量化算子。

## 5. P4：建立真实 API workflow

- [x] 从已经在 ComfyUI UI 中成功执行的真实工作流导出 API JSON，禁止手写猜测节点图。
- [x] 重建 `minimax_h3_hongchao_a2a_12gb.api.json` 与 sidecar manifest。
- [x] manifest 为每个 slot 记录真实 node id、input name、数据类型和可选性：
  - positive/negative prompt；
  - first/last frame；
  - 1–3 张 character refs；
  - 1 条 motion/video ref；
  - width/height/frames/fps/steps/seed；
  - output prefix。
- [x] 使用 `/object_info` 验证所有 class type、input 和枚举值（全量 14 个节点通过）。
- [x] 验证 `UNETLoader`、Text Encoder loader、VAE loader 返回类型与下游节点完全一致（`execution.validate_prompt` 校验通过）。
- [x] 缺任意模型或节点时 preflight 失败，不允许静默删除参考输入继续生成。
- [x] API workflow 使用的模型文件名与 checksum 清单完全一致。

## 6. P5：ComfyUI 服务与能力预检

- [x] 使用目标 GPU 启动 `D:\ComfyUI\start_comfyui.bat`，不得用 `--cpu` 结果替代 GPU 验收。
- [x] `GET /system_stats` 成功并确认设备为 RTX 3060 12GB (VRAM 12.8GB，可用 11.8GB)。
- [x] `GET /object_info` 至少确认真实 H3 loader/encoder/sampler/output 节点全部存在。
- [x] 模型列表接口能看到完整且校验通过的 diffusion、text encoder 和 VAE。
- [x] 检查启动日志：无 custom node import error、missing model、unsupported architecture 或 dtype 错误。
- [x] 做一次只加载模型的预检，记录峰值 VRAM、RAM、耗时和磁盘活动（4个模型实测全部成功加载，动态卸载与 RAM 管理正常）。
- [x] 确认 `--lowvram`/dynamic offload 参数组合；是否增加 `--disable-pinned-memory --cache-none` 由实测决定（确认在 32GB RAM + 12GB VRAM 下启用 `--lowvram` 动态卸载生效）。

## 7. P6：黄金输入真实 smoke test

- [ ] 使用既有黄金输入：
  - `canonical_keyframe_k.png`；
  - `golden_motion_reference_5s_24fps.mp4`；
  - 同版本人物参考；
  - First Frame = Last Frame = K。
- [ ] 第一条只跑 864×480、5 秒、24fps、单任务、固定 seed。
- [ ] 任务必须取得非空 `comfy_prompt_id`，并能在 ComfyUI queue/history 中追溯。
- [ ] ComfyUI history 必须返回真实视频 output；不得由 NovaStory catch 分支合成。
- [ ] 保存 raw 视频、工作流、完整 prompt、seed、模型/节点指纹和资源峰值。
- [ ] 用抽帧或光流确认画面存在模型生成的角色/环境运动，不是整帧统一缩放和平移。
- [ ] 验证人物身份、服装、额饰、脸部、闭唇、镜头和光色稳定性。
- [ ] 验证输出可解码、864×480、24fps、最终恰好 120 帧、5 秒、H.264/yuv420p、无音轨。
- [ ] smoke test 未通过前，不运行 720p、2K、批量或章节导出。

## 8. P7：LoopCloser 与生产稳定性

- [ ] 保留 raw，LoopCloser 只生成派生 final，不覆盖原始证据。
- [ ] 评分至少包含 Appearance、Pose、Motion、Flicker；不得只比较首尾单帧 MAE。
- [ ] raw/final 并排检查 0.5×、1×、2×，连续播放 10 圈。
- [ ] 固定 spec 和 seed 连续生成至少 5 次：无 OOM、无服务崩溃、无假 fallback。
- [ ] 记录每次模型加载、推理、解码、后处理耗时及峰值 VRAM/RAM。
- [ ] 建立 `pass / manual_review / reject` 三档阈值；阈值来自真实黄金样片标注。
- [ ] 通过黄金 Body Master 后，再开始 mouth box、landmarks、viseme patches 和实时嘴层制作。

## 9. Definition of Done

只有同时满足以下条件，才能把状态改为“红潮 Hybrid H3 A2A 已安装可用”：

- [ ] 红潮名称对应的实际权重或 workflow 身份已澄清并可追溯。
- [ ] 所有模型文件完成 SHA-256 校验，许可已记录。
- [ ] 使用真实、可追溯的 H3/Ref2VA 实现，不使用 metadata-only 占位 adapter。
- [ ] GPU 模式下 ComfyUI `/system_stats`、`/object_info`、模型预检全部通过。
- [ ] NovaStory 得到真实 `comfy_prompt_id` 和 ComfyUI video history output。
- [ ] 真实黄金样片生成成功，且证明确非 FFmpeg 静态运镜。
- [ ] 连续 5 次 480p/5s 任务无 OOM、崩溃或 fallback。
- [ ] raw/final/QA/manifest/模型与节点指纹完整可追溯。
- [ ] 失败路径不会再向 UI 报告“H3 视频生成成功”。

## 10. 核验依据

- `C:\Users\Administrator\.gemini\antigravity\brain\728c1eaf-de44-4b27-aca6-6bb1cac4ba0f\walkthrough.md`
- `D:\ComfyUI\download_h3_models.py`
- `D:\ComfyUI\custom_nodes\ComfyUI-MiniMax-H3\nodes_h3.py`
- `D:\ComfyUI\custom_nodes\ComfyUI-VideoHelperSuite\pyproject.toml`
- `D:\StudioProjects\nova-story\backend\src\services\video\video_generation_service.ts`
- `D:\StudioProjects\nova-story\backend\src\services\video\video_postprocess_service.ts`
- `D:\StudioProjects\nova-story\backend\logs\novastory.log`
- `D:\StudioProjects\GoddessDaily\local-runtime\docs\红潮 Hybrid H3 A2A：视频参考 + 人物参考.md`
