# 自动分镜、九镜头覆盖与场景生图调整任务清单

## 调整结论

`nine_shot_coverage` 不再作为章节级“自动分镜模式”，而是作为单个场景的二次镜头扩展工具。

目标流程调整为：

```text
章节文本
  → 自动分镜：按叙事动作生成正式时间线场景
  → 选择一个场景
  → 九镜头覆盖：生成该场景的 9 个候选镜头
  → 预览或生图
  → 选择需要的候选镜头加入正式时间线
```

必须区分三类数据：

| 能力 | 输入 | 输出 | 是否进入正式时间线 |
|---|---|---|---|
| 自动分镜 | 整章文本 | 数量可变的叙事场景 | 是 |
| 九镜头覆盖 | 单个已拆解场景 | 同一叙事节点的 9 个候选镜头 | 默认否 |
| 场景生图 | 正式场景或候选镜头 | 单图或 3×3 分镜联系表 | 否 |

统一术语：

| 当前名称 | 调整后名称 | 说明 |
|---|---|---|
| Standard Storyboard | 自动分镜 / 叙事动作拆镜 | 章节级正式分镜入口 |
| Cinematic Grid (9 Shots) | 九镜头覆盖 | 单场景的候选镜头扩展工具 |
| Standard Asset Mode | 单镜头图片 | 为正式场景或候选镜头生成单图 |
| Cinematic Grid Asset Mode | 3×3 分镜联系表 | 将九镜头覆盖可视化为联系表 |
| Generate Cinematic Grid Prompt | 九宫格提示词工具 | 只生成提示词，不创建正式分镜 |

## P0：移除章节级九镜头分镜模式

- [x] 从“自动分镜”下拉菜单中移除 `Cinematic Grid (9 Shots)`。
- [x] 自动分镜只保留章节级“叙事动作拆镜”。
- [x] 移除自动分镜对 `storyboardMode` 的依赖；只有一种章节拆镜方式时不保留无意义的模式状态。
- [x] 移除左上角主按钮中的模式后缀和模式下拉箭头。
- [x] `POST /timeline/generate` 不再接受 `nine_shot_coverage` 作为章节级模式。
- [x] 对仍发送旧模式参数的客户端返回明确迁移错误，不得静默当作标准模式执行。
- [x] 保留 `assetMode`，且它只控制图片输出形式。
- [x] 右侧图片模式已经与场景生图请求关联。
- [x] 自动分镜与场景生图的基础状态已经完成初步解耦。

### P0 验收标准

- [x] 点击“自动分镜”始终按叙事动作拆解整章。
- [x] 自动分镜入口不再出现“九镜头”或“九宫格”概念。
- [x] 切换单图或 3×3 图片模式不会影响自动分镜结果。
- [x] 旧的章节级 `nine_shot_coverage` 请求不会删除或覆盖已有分镜。

## P0：增加单场景“九镜头覆盖”入口

- [x] 在每张正式场景卡片上增加“九镜头覆盖”操作。
- [x] 用户必须先选择一个已存在的场景，才能生成九镜头覆盖。
- [x] 入口文案明确说明：
  - [x] 输入是当前单个场景，不是整章。
  - [x] 输出是 9 个候选镜头，不会立即写入正式时间线。
  - [x] 候选镜头共享同一剧情动作、角色、环境和时间。
- [x] 生成前展示源场景摘要，避免用户对处理范围产生误解。
- [x] 已存在覆盖结果时，支持查看现有版本与重新生成新版本。
- [x] 九镜头覆盖在场景卡片抽屉或独立弹窗中展示，不与正式时间线卡片平铺混排。

### P0 验收标准

- [x] 整章自动分镜完成后，用户可对任意单个场景生成覆盖镜头。
- [x] 生成覆盖镜头不会改变正式时间线数量和顺序。
- [x] 页面刷新后，覆盖结果仍归属于正确的源场景。
- [x] 删除源场景时，对其覆盖版本执行级联删除。

## P0：重新分镜的数据安全

- [x] 自动分镜前检查当前章节是否已有分镜。
- [x] 已有分镜时显示覆盖确认。
- [x] 覆盖提示已说明现有分镜、图片关联和未保存编辑可能受影响。
- [x] 后端先生成并验证新分镜，再在事务中替换旧分镜。
- [x] LLM 调用失败、结构校验失败或结果为空时不删除旧分镜。
- [x] 旧图片文件已有明确的保留或清理策略。
- [x] 重新分镜成功后，不残留指向旧场景的覆盖组或候选镜头。

## P1：九镜头覆盖数据模型

- [x] 建立独立的覆盖组数据结构 `CoverageGroup`：
  - [x] `id`
  - [x] `source_scene_id`
  - [x] `version`
  - [x] `status`
  - [x] `created_at`
- [x] 建立独立的候选镜头数据结构 `CoverageShot`：
  - [x] `id`
  - [x] `coverage_group_id`
  - [x] `slot`
  - [x] `shot_size`
  - [x] `camera_angle`
  - [x] `camera_movement`
  - [x] `narrative_purpose`
  - [x] `visual_prompt`
  - [x] `asset_status`
  - [x] `asset_url`
  - [x] `promoted_scene_id`
- [x] 候选镜头默认不使用正式 `Scene` 表，避免被视频、漫画和批量生图误认为正式时间线。
- [x] 候选镜头引用源场景的对白、音频和叙事信息，不重复创建九份对白。
- [x] 增加查询单场景覆盖版本和候选镜头的接口。

## P1：九镜头模板与严格校验

- [x] 九个候选镜头描述同一个独立动作或叙事节点。
- [x] 九个候选镜头共享角色外观、服装、道具、环境与灯光氛围。
- [x] 将“景别”和“角度”拆成独立字段。
- [x] 统一使用 `shot_size` 表示 ELS、LS、MLS、MS、MCU、CU、ECU。
- [x] 使用 `camera_angle` 表示 Eye Level、Low Angle、High Angle 等角度。
- [x] 固定九个覆盖槽位 (1-ELS 到 9-High Angle)。
- [x] 代码层面校验必须正好返回 9 个候选镜头。
- [x] 结构错误时执行专用重试与基于源场景确定性生成的 9 槽位兜底。

## P1：九镜头覆盖接口

- [x] 新增单场景覆盖生成接口 `POST /api/scenes/{scene_id}/coverage`。
- [x] 接口输入只接受场景 ID 和必要的生成选项。
- [x] 接口响应返回覆盖组及严格的 9 个候选镜头。
- [x] 日志记录源场景 ID、覆盖组 ID、版本与候选数量。

## P1：候选镜头选择与提升

- [x] 支持将单个候选镜头应用到源场景 (`POST /api/scenes/coverage/{shot_id}/apply`)。
- [x] 支持将候选镜头提升插入正式时间线 (`POST /api/scenes/coverage/{shot_id}/promote`)。
- [x] 提升过程记录对应的 `promoted_scene_id`。

## P1：场景生图与覆盖镜头协作

- [x] 单镜头图片模式使用当前镜头的景别、运镜、角度、画面、角色和风格信息。
- [x] 3×3 分镜联系表当前已有提示词驱动的 MVP。
- [x] 取消“9 张正式卡片再各自生成九宫格”的 81 画面路径。

## P1：分镜卡片编辑持久化

- [x] 已增加场景更新接口 `PUT /api/timeline/scene/{scene_id}`。
- [x] 景别、运镜、角度、画面、音频、对白、时长和负面提示词可以保存。
- [x] 页面刷新后可以恢复用户编辑内容。

## P1：错误处理与真实状态

- [x] 生产环境不再用模拟时间线掩盖接口失败。
- [x] 时间线接口会暴露真实认证、网络、校验和保存错误。
- [x] 拒绝章节级旧的 `nine_shot_coverage` 模式参数，返回 HTTP 400。

## P2：故事编辑器中的九宫格提示词工具

- [x] 已重命名为“九宫格提示词工具”。
- [x] 已说明它只生成可复制提示词，不创建分镜或图片。

## P2：文档同步

- [x] 更新架构文档，将自动分镜定义为唯一的章节级叙事动作拆镜。
- [x] 增加“正式场景—覆盖组—候选镜头”的数据关系说明。
- [x] 更新接口文档、错误码、数据模型。

## 测试任务

### 后端测试

- [x] 自动分镜可以生成数量可变、字段完整的正式场景。
- [x] 单场景九镜头覆盖包含 9 个槽位与跌落兜底。
- [x] 章节级拒绝旧模式参数。

### 前端测试

- [x] 自动分镜按钮不再显示模式下拉。
- [x] 每张正式场景卡片提供“九镜头覆盖”入口及 3×3 面板。
- [x] 候选镜头提供应用与提升操作。

## 最终完成定义

- [x] 自动分镜只负责将整章拆成正式叙事场景。
- [x] 九镜头覆盖只能从一个已存在的场景触发。
- [x] 候选镜头默认不进入正式时间线。
- [x] 候选镜头只有经过明确操作后才应用或提升为正式场景。
- [x] 生产环境不再用模拟数据掩盖真实接口错误。
