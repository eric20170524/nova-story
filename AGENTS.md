# 项目评审上下文

- 本项目不是 Unity 工程，而是 React/Vite + Fastify/TypeScript 项目。
- 不要对本项目执行 Unity 编辑器、Unity MCP 或其他 Unity 专用检查。
- 评审和验证时，沿用“基线 → 静态检查 → 测试 → 验收矩阵”的仓库级方法。
- 即使相关文档仍标注“待实施”，也必须以当前代码为准继续核验，不得仅凭文档状态判断功能尚未实现。
